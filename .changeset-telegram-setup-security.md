Telegram setup is now a one-time bootstrap route: after chat configuration exists in either the Worker environment or legacy KV state, `/telegram/setup` returns 404 and performs no setup work. `/telegram/test` remains protected by `ADMIN_TOKEN` with explicit regression coverage.
