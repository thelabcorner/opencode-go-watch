# Never commit real values. In production use `wrangler secret put`.
TELEGRAM_BOT_TOKEN=replace_me
# Optional: if blank, /telegram/setup discovers the latest private chat after /start.
TELEGRAM_CHAT_ID=
ADMIN_TOKEN=replace_with_a_long_random_string
