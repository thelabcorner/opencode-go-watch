{
  "name": "opencode-go-watch",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that semantically monitors OpenCode Go usage limits and sends rich Telegram diffs.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:coverage": "node --test --experimental-test-coverage test/*.test.js",
    "check": "for f in src/*.js test/*.test.js bench/*.mjs; do node --check \"$f\" || exit 1; done",
    "validate": "npm run check && npm run typecheck && npm test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf:types": "wrangler types",
    "typecheck": "tsc --allowJs --checkJs --noEmit --target ES2023 --module ESNext --moduleResolution Bundler --lib ES2023,DOM --skipLibCheck src/*.js",
    "bench": "node bench/perf.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.102.0",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  },
  "cloudflare": {
    "bindings": {
      "TELEGRAM_BOT_TOKEN": {
        "description": "Telegram Bot API token. Store as an encrypted Worker secret; never commit it."
      },
      "ADMIN_TOKEN": {
        "description": "Long random secret protecting /status, /check, /telegram/setup, and baseline controls."
      }
    }
  }
}
