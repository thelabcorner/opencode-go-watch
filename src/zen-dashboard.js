const CSS = String.raw`
:root{color-scheme:dark;font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif;--bg:#09090b;--p:#0d0d10;--p2:#111114;--b:#27272a;--l:#1d1d21;--t:#f4f4f5;--m:#71717a;--f:#52525b;--v:#a78bfa;--g:#4ade80;--a:#fbbf24;--r:#fb7185}
*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;min-width:320px;background:var(--bg);color:var(--t)}a{color:inherit;text-decoration:none}button,input,select{font:inherit}.wrap{width:min(1240px,calc(100% - 48px));margin:auto}
.top{position:sticky;top:0;z-index:20;height:52px;border-bottom:1px solid var(--l);background:#09090bf2;backdrop-filter:blur(14px)}.topin{width:min(1380px,calc(100% - 32px));height:100%;margin:auto;display:flex;align-items:center;gap:10px}.brand{display:flex;align-items:center;gap:9px}.mark{width:24px;height:24px;display:grid;place-items:center;border:1px solid #49356f;border-radius:5px;background:#191329;color:#c4b5fd;font:700 11px ui-monospace,monospace}.brand b{font:650 13px ui-monospace,monospace}.nav{margin-left:30px;display:flex;height:100%}.nav a{padding:0 10px;display:flex;align-items:center;color:var(--m);font-size:11px}.nav a:hover{color:white}.status{margin-left:auto;display:flex;align-items:center;gap:7px;color:#a1a1aa;font-size:11px}.dot{width:6px;height:6px;border-radius:50%;background:var(--g)}.dot.bad{background:var(--r)}.dot.wait{background:var(--a)}
.hero{padding:44px 0 28px;display:flex;align-items:flex-end;justify-content:space-between;gap:24px}.kick{color:var(--m);font-size:10px;font-weight:650;letter-spacing:.11em;text-transform:uppercase}.hero h1{margin:8px 0 0;font-size:clamp(27px,3vw,35px);letter-spacing:-.045em}.hero p{margin:9px 0 0;max-width:720px;color:var(--m);font-size:12px;line-height:1.55}.verify{text-align:right}.verify small,.verify strong{display:block}.verify small{color:var(--f);font-size:8px;text-transform:uppercase}.verify strong{margin-top:4px;color:#d4d4d8;font-size:11px}.err{margin-top:12px;padding:9px;border:1px solid #66303c;border-radius:5px;background:#26141a;color:#fda4af;font-size:9px}
.stats{display:grid;grid-template-columns:repeat(4,1fr);border:1px solid var(--b);border-radius:7px;background:var(--p);overflow:hidden}.stat{padding:18px}.stat+.stat{border-left:1px solid var(--l)}.stat span,.stat b,.stat em{display:block}.stat span{color:var(--f);font-size:8px;text-transform:uppercase;letter-spacing:.08em}.stat b{margin-top:7px;font:560 23px ui-monospace,monospace}.stat em{margin-top:4px;color:var(--m);font-size:8px;font-style:normal}
.section{padding-top:46px}.head{margin-bottom:13px;display:flex;justify-content:space-between;align-items:flex-end;gap:16px}.head h2{margin:6px 0 0;font-size:15px}.head p{margin:5px 0 0;color:var(--f);font-size:9px}.surface{border:1px solid var(--b);border-radius:7px;background:var(--p);overflow:hidden}.surface-title{min-height:42px;padding:0 14px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--l);color:var(--f);font-size:8px}.surface-title strong{color:#a1a1aa;font-size:9px;text-transform:uppercase;letter-spacing:.09em}
.freegrid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px;padding:11px}.freecard{min-height:122px;padding:13px;border:1px solid #214d31;border-radius:6px;background:#0e1912}.freehead{display:flex;align-items:center;gap:8px}.logo{width:26px;height:26px;flex:none;display:grid;place-items:center;border:1px solid #303038;border-radius:5px;background:#151519;overflow:hidden;color:#888;font:700 9px ui-monospace,monospace}.logo img{width:17px;height:17px;object-fit:contain;filter:brightness(0) invert(1);opacity:.9}.freecard h3{margin:0;font-size:10px}.freecard code{display:block;margin-top:8px;color:#86efac;font-size:8px;word-break:break-all}.freecard p{margin:7px 0 0;color:#6f8d78;font-size:8px;line-height:1.45}
.badge{display:inline-flex;padding:2px 5px;border:1px solid #2c5c3b;border-radius:4px;background:#102217;color:#86efac;font-size:7px}.badge.v{border-color:#49356f;background:#191329;color:#c4b5fd}.badge.a{border-color:#5c4a22;background:#211b0c;color:#fcd34d}.badge.r{border-color:#66303c;background:#26141a;color:#fda4af}
.alerts{display:grid;grid-auto-flow:column;grid-auto-columns:minmax(280px,35%);gap:9px;padding:11px;overflow-x:auto;scroll-snap-type:inline mandatory}.alert{scroll-snap-align:start;padding:13px;border:1px solid var(--l);border-radius:6px;background:#0b0b0e}.alert small{color:var(--f);font-size:8px}.alert h3{margin:9px 0 0;font-size:10px}.alert p{margin:6px 0 0;color:var(--m);font-size:8px;line-height:1.5}
.toolbar{padding:10px;display:flex;gap:8px;border-bottom:1px solid var(--l)}.search{flex:1;height:32px;padding:0 9px;border:1px solid var(--b);border-radius:5px;background:#0a0a0c;color:#ddd;font-size:10px}.toolbar select{height:32px;padding:0 8px;border:1px solid var(--b);border-radius:5px;background:var(--p2);color:#aaa;font-size:9px}.filters{padding:7px 10px;display:flex;gap:5px;overflow:auto;border-bottom:1px solid var(--l)}.filter{height:26px;padding:0 8px;flex:none;border:1px solid transparent;border-radius:4px;background:transparent;color:var(--m);font-size:8px;cursor:pointer}.filter.active{border-color:#333338;background:#18181b;color:white}
.tablewrap{overflow-x:auto}.table{width:100%;border-collapse:collapse}.table th{height:35px;padding:0 10px;border-bottom:1px solid var(--b);color:var(--f);font-size:7px;text-align:left;text-transform:uppercase;letter-spacing:.06em;white-space:nowrap}.table td{padding:10px;border-bottom:1px solid var(--l);color:#aaa;font-size:8px;vertical-align:middle}.model{display:flex;align-items:center;gap:9px;min-width:190px}.model strong,.model small{display:block}.model strong{color:#d4d4d8;font-size:9px}.model small{margin-top:3px;color:var(--f);font-size:7px}.mono{font-family:ui-monospace,monospace;font-variant-numeric:tabular-nums}.prices{display:grid;gap:4px;min-width:230px}.price{display:grid;grid-template-columns:minmax(70px,1fr) repeat(4,54px);gap:5px;align-items:center}.variant{color:var(--f);font-size:7px}.freeprice{color:#86efac}.endpoint{max-width:220px;word-break:break-word;line-height:1.4}.empty{padding:28px;text-align:center;color:var(--m);font-size:9px}
.cards{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;padding:11px}.card{padding:11px;border:1px solid var(--l);border-radius:5px;background:#0b0b0e}.card strong{font-size:9px}.card small{display:block;margin-top:5px;color:var(--f);font-size:8px;line-height:1.5}.foot{margin-top:50px;border-top:1px solid var(--l)}.footin{min-height:70px;display:flex;align-items:center;justify-content:space-between;color:var(--f);font-size:8px}.foot a{color:var(--m)}
@media(max-width:1000px){.freegrid{grid-template-columns:repeat(3,1fr)}}
@media(max-width:820px){.wrap{width:calc(100% - 28px)}.stats{grid-template-columns:repeat(2,1fr)}.stat:nth-child(3){border-left:0;border-top:1px solid var(--l)}.stat:nth-child(4){border-top:1px solid var(--l)}.freegrid{grid-template-columns:repeat(2,1fr)}.alerts{grid-auto-columns:minmax(270px,78%)}.cards{grid-template-columns:repeat(2,1fr)}.nav{display:none}.hero{align-items:flex-start;flex-direction:column}.verify{text-align:left}.table thead{display:none}.table,.table tbody,.table tr,.table td{display:block;width:100%}.table tbody{padding:8px}.table tr{margin-bottom:8px;padding:10px;border:1px solid var(--l);border-radius:6px;background:#0b0b0e}.table td{min-height:28px;padding:5px 0;display:grid;grid-template-columns:92px 1fr;border:0;text-align:right!important}.table td:before{content:attr(data-label);color:var(--f);font-size:7px;text-align:left;text-transform:uppercase}.table td:first-child{grid-template-columns:1fr;padding-bottom:9px;border-bottom:1px solid var(--l);text-align:left!important}.table td:first-child:before{display:none}.prices{justify-items:end}.price{grid-template-columns:minmax(65px,1fr) repeat(4,50px)}}
@media(max-width:540px){.wrap{width:calc(100% - 22px)}.stat{padding:14px}.freegrid{grid-template-columns:1fr}.cards{grid-template-columns:1fr}.toolbar{flex-wrap:wrap}.search{width:100%;flex-basis:100%}.toolbar select{flex:1;min-width:0}.surface-title{padding:10px;align-items:flex-start;gap:6px;flex-direction:column}.alerts{grid-auto-columns:88%}.footin{padding:16px 0;gap:10px;align-items:flex-start;flex-direction:column}.price{grid-template-columns:62px repeat(4,43px);font-size:7px}}
`;

/** @type {Array<[RegExp,{name:string,logo:string}]>} */
const PROVIDERS = [
  [/^GPT/i, { name: "OpenAI", logo: "openai" }],
  [/^Claude/i, { name: "Anthropic", logo: "anthropic" }],
  [/^Gemini/i, { name: "Google", logo: "google" }],
  [/^Grok/i, { name: "xAI", logo: "xai" }],
  [/^Muse/i, { name: "Meta", logo: "meta" }],
  [/^Qwen/i, { name: "Alibaba", logo: "alibaba" }],
  [/^DeepSeek/i, { name: "DeepSeek", logo: "deepseek" }],
  [/^MiniMax/i, { name: "MiniMax", logo: "minimax" }],
  [/^GLM/i, { name: "Zhipu AI", logo: "zhipuai" }],
  [/^Kimi/i, { name: "Moonshot AI", logo: "moonshotai" }],
  [/^MiMo/i, { name: "Xiaomi", logo: "xiaomi" }],
  [/^Hy3/i, { name: "Tencent", logo: "tencent" }],
  [/^Nemotron/i, { name: "NVIDIA", logo: "nvidia" }],
  [/^Laguna/i, { name: "Poolside", logo: "poolside" }],
];

const esc = (value) => String(value ?? "").replace(/[&<>\"]/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" })[c]);
const provider = (name) => { for (const [re, item] of PROVIDERS) if (re.test(name)) return item; return { name: "Stealth / unknown", logo: "" }; };
const logo = (item) => item.logo ? `<span class="logo"><img loading="lazy" referrerpolicy="no-referrer" src="https://models.dev/logos/${encodeURIComponent(item.logo)}.svg" alt="${esc(item.name)} logo"></span>` : `<span class="logo">?</span>`;
const money = (value) => typeof value === "number" && Number.isFinite(value) ? (value === 0 ? "Free" : `$${value.toLocaleString("en-US", { maximumFractionDigits: 6 })}`) : "—";

function priceMarkup(model) {
  const rows = Array.isArray(model?.pricing) ? model.pricing : [];
  if (!rows.length) return `<span class="variant">Not listed in docs pricing table</span>`;
  return `<div class="prices">${rows.map((row) => {
    const base = String(row.label ?? "").replace(/\s*\([^)]*\)\s*$/, "").trim();
    const variant = String(row.label ?? "").slice(base.length).trim().replace(/^\(|\)$/g, "") || "Standard";
    return `<div class="price"><span class="variant">${esc(variant)}</span><span class="mono ${row.free ? "freeprice" : ""}">${money(row.inputPerM)}</span><span class="mono ${row.free ? "freeprice" : ""}">${money(row.outputPerM)}</span><span class="mono ${row.free ? "freeprice" : ""}">${money(row.cachedReadPerM)}</span><span class="mono ${row.free ? "freeprice" : ""}">${money(row.cachedWritePerM)}</span></div>`;
  }).join("")}</div>`;
}

function historyCards(history) {
  const zen = history.filter((event) => String(event.kind ?? "").startsWith("zen-"));
  if (!zen.length) return `<div class="empty">No Zen alerts archived yet. The first model, free-tier, pricing, deprecation, discount, or operational event will appear here.</div>`;
  return `<div class="alerts">${zen.slice(0, 18).map((event) => `<article class="alert"><small data-relative="${esc(event.at)}">${esc(event.at)}</small><h3>${esc(event.title)}</h3><p>${esc(event.detail || event.message)}</p></article>`).join("")}</div>`;
}

export function zenDashboard(status, history = []) {
  const snapshot = status.snapshot;
  const models = Object.values(snapshot?.models ?? {});
  const free = models.filter((model) => model.free);
  const paid = models.filter((model) => !model.free);
  const apiOnly = snapshot?.consistency?.apiOnly ?? [];
  const deprecated = Object.entries(snapshot?.docs?.deprecated ?? {});
  const offers = snapshot?.docs?.offers ?? [];
  const meta = status.meta ?? {};
  const state = status.error ? "Degraded" : snapshot ? "Tracking" : "Waiting for baseline";
  const stateClass = status.error ? "bad" : snapshot ? "" : "wait";
  const providers = [...new Set(models.map((model) => provider(model.name).name))].sort();

  const freeCards = free.map((model) => {
    const maker = provider(model.name);
    return `<article class="freecard"><div class="freehead">${logo(maker)}<div><h3>${esc(model.name)}</h3><span class="badge">FREE NOW</span></div></div><code>opencode/${esc(model.id)}</code><p>${model.documented ? "Documented Zen endpoint + pricing" : "Available in Zen models API; documentation may be catching up"}</p></article>`;
  }).join("");

  const rows = models.map((model) => {
    const maker = provider(model.name);
    const filters = [model.free ? "free" : "paid", model.documented ? "documented" : "api-only"].join(" ");
    return `<tr data-name="${esc((model.name + " " + model.id + " " + maker.name).toLowerCase())}" data-provider="${esc(maker.name)}" data-filter="${filters}"><td data-label="Model"><div class="model">${logo(maker)}<div><strong>${esc(model.name)}</strong><small>${esc(maker.name)} · <code>opencode/${esc(model.id)}</code>${model.ownedBy ? ` · owner ${esc(model.ownedBy)}` : ""}</small></div></div></td><td data-label="Status">${model.free ? '<span class="badge">FREE</span>' : '<span class="badge v">PAID</span>'} ${model.documented ? "" : '<span class="badge a">API ONLY</span>'}</td><td data-label="Pricing">${priceMarkup(model)}</td><td data-label="Endpoint" class="endpoint mono">${model.endpoint ? esc(model.endpoint.replace("https://opencode.ai/zen/v1", "…/zen/v1")) : "—"}</td></tr>`;
  }).join("");

  const offerSection = offers.length ? `<section class="section wrap" id="offers"><div class="head"><div><div class="kick">Discount / promotion intelligence</div><h2>Current Zen offers</h2><p>Promotion and discount wording is tracked semantically in addition to numeric price changes.</p></div></div><div class="surface"><div class="cards">${offers.map((offer) => `<article class="card"><span class="badge a">OFFER</span><small>${esc(offer)}</small></article>`).join("")}</div></div></section>` : "";

  return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#09090b"><title>OpenCode Zen Watch</title><style>${CSS}</style><script src="/zen-dashboard.js" defer></script></head><body><header class="top"><div class="topin"><a class="brand" href="/zen"><span class="mark">Z</span><b>zen/watch</b></a><nav class="nav"><a href="/">Go Watch</a><a href="#free">Free</a><a href="#offers">Offers</a><a href="#models">Models</a><a href="#alerts">Alerts</a><a href="#deprecated">Deprecated</a></nav><div class="status"><i class="dot ${stateClass}"></i>${esc(state)}</div></div></header><main><section class="hero wrap"><div><div class="kick">OpenCode Zen semantic intelligence</div><h1>Zen model tracker</h1><p>Tracks the authoritative Zen models API plus Zen documentation: free availability, model additions/removals, discounts, every published pricing variant, endpoint routing, deprecations, and unknown semantic changes. The /zen marketing page is intentionally not monitored.</p>${status.error ? `<div class="err">${esc(status.error.message)}</div>` : ""}</div><div class="verify"><small>Last persisted heartbeat</small><strong data-relative="${esc(meta.lastSuccessAt ?? "")}">${esc(meta.lastSuccessAt ?? "not yet")}</strong></div></section><section class="stats wrap"><div class="stat"><span>Available API models</span><b>${models.length}</b><em>Zen /v1/models is availability truth</em></div><div class="stat"><span>Free now</span><b>${free.length}</b><em>Additions/removals get priority alerts</em></div><div class="stat"><span>Paid models</span><b>${paid.length}</b><em>Every pricing variant tracked</em></div><div class="stat"><span>API-only / docs lag</span><b>${apiOnly.length}</b><em>Cross-source consistency</em></div></section>${offerSection}<section class="section wrap" id="free"><div class="head"><div><div class="kick">Highest-priority signal</div><h2>Currently free Zen models</h2><p>New free additions, removals, and free→paid transitions receive dedicated Telegram headlines.</p></div><a href="https://opencode.ai/docs/zen/#pricing" class="badge v">Zen pricing ↗</a></div><div class="surface"><div class="surface-title"><strong>Free availability</strong><span>${free.length} free model${free.length === 1 ? "" : "s"}</span></div><div class="freegrid">${freeCards || '<div class="empty">No free models detected.</div>'}</div></div></section><section class="section wrap" id="alerts"><div class="head"><div><div class="kick">Brotli-backed history</div><h2>Recent Zen alerts</h2><p>Uses the same capped compressed alert archive as Go Watch; unchanged checks create no history writes.</p></div></div><div class="surface">${historyCards(history)}</div></section><section class="section wrap" id="models"><div class="head"><div><div class="kick">Authoritative availability + docs economics</div><h2>All available Zen models</h2><p>API availability is authoritative; documentation enriches names, endpoints, SDK routing, and all published pricing variants.</p></div></div><div class="surface"><div class="toolbar"><input class="search" id="search" type="search" placeholder="Search model, id, or provider…"><select id="provider"><option value="">All providers</option>${providers.map((item) => `<option>${esc(item)}</option>`).join("")}</select></div><div class="filters" id="filters"><button class="filter active" data-filter="all">All ${models.length}</button><button class="filter" data-filter="free">Free ${free.length}</button><button class="filter" data-filter="paid">Paid ${paid.length}</button><button class="filter" data-filter="api-only">API-only ${apiOnly.length}</button></div><div class="tablewrap"><table class="table"><thead><tr><th>Model</th><th>Status</th><th>Pricing variants · input / output / cached read / write</th><th>Endpoint</th></tr></thead><tbody id="rows">${rows}</tbody></table><div class="empty" id="empty" hidden>No matching Zen models.</div></div></div></section><section class="section wrap" id="deprecated"><div class="head"><div><div class="kick">Lifecycle</div><h2>Deprecated models</h2><p>New deprecations, removals, and date changes are semantic alerts.</p></div></div><div class="surface"><div class="cards">${deprecated.map(([name, date]) => `<article class="card"><strong>${esc(name)}</strong><small>${esc(date)}</small></article>`).join("") || '<div class="empty">No deprecated model rows detected.</div>'}</div></div></section></main><footer class="foot"><div class="footin wrap"><span>Checks every minute · API + docs · fail-closed · residual fallback</span><div><a href="/zen/health">Health JSON</a> · <a href="https://opencode.ai/docs/zen/">Zen docs</a></div></div></footer></body></html>`;
}

export const zenDashboardScript = String.raw`(()=>{const $=s=>document.querySelector(s),all=s=>[...document.querySelectorAll(s)],search=$('#search'),provider=$('#provider'),empty=$('#empty');let filter='all';function rows(){return all('#rows tr')}function apply(){const q=(search?.value||'').toLowerCase().trim(),p=provider?.value||'';let visible=0;for(const row of rows()){const match=(!q||row.dataset.name.includes(q))&&(!p||row.dataset.provider===p)&&(filter==='all'||(row.dataset.filter||'').split(' ').includes(filter));row.hidden=!match;if(match)visible++}if(empty)empty.hidden=visible!==0}search?.addEventListener('input',apply);provider?.addEventListener('change',apply);$('#filters')?.addEventListener('click',e=>{const b=e.target.closest('[data-filter]');if(!b)return;filter=b.dataset.filter;all('.filter').forEach(x=>x.classList.toggle('active',x===b));apply()});function relative(){all('[data-relative]').forEach(el=>{const t=Date.parse(el.dataset.relative);if(!Number.isFinite(t))return;const s=Math.max(0,(Date.now()-t)/1000|0);el.textContent=s<60?s+'s ago':s<3600?(s/60|0)+'m ago':s<86400?(s/3600|0)+'h ago':(s/86400|0)+'d ago'})}relative();setInterval(relative,30000);document.addEventListener('keydown',e=>{if(e.key==='/'&&!['INPUT','SELECT','TEXTAREA'].includes(document.activeElement?.tagName)){e.preventDefault();search?.focus()}});apply()})();`;
